package practice.problems.Strings;

public class LongestPalindromicString {

    public void executeLongestPalindromicString(){

    }

    /*private String longestPalindromicString(String s){
        int left =0;
        int len= s.length();
        StringBuilder sba = new StringBuilder();
        StringBuilder sbb = new StringBuilder();
        for(int right=s.length()-1;right>=0;right--){
            if(sba.append(s, left, right).compareTo(sbb.reverse()) == 0){
                return sba.toString();
            }
            sbb = new StringBuilder(sba);
        }
        sba.append(s, left, right);
        sbb.append(s,)
        if(sb.)
    }*/
}
